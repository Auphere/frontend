name: Deploy Frontend to AWS

on:
  push:
    branches:
      - main
  workflow_dispatch:

# NOTE: Docker deployment has been removed.
# This workflow is currently disabled.
# If you need to deploy, configure a new deployment method (e.g., S3 + CloudFront, Vercel, Netlify, etc.)

jobs:
  # Docker build and push job removed - Dockerfile no longer exists
  # build-and-push:
  #   name: Build and Push to ECR
  #   runs-on: ubuntu-latest
  #   ...

  # App Runner deployment job removed - requires Docker images
  # deploy:
  #   name: Deploy to App Runner
  #   runs-on: ubuntu-latest
  #   ...

  placeholder:
    name: Workflow placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build frontend (no deployment)
        run: |
          echo "⚠️  Docker deployment has been removed."
          echo "📦 Building frontend for verification only..."
          npm ci
          npm run build
          echo "✅ Build completed successfully. No deployment configured."
